# Demo UI — copy to `apps/demo-ui/.env` for local overrides (optional).
# Vite exposes only variables prefixed with VITE_.

# Base URL of the demo API (includes `/api` path prefix). Example matches demo-api default + global prefix.
VITE_API_BASE_URL=http://localhost:3000/api
